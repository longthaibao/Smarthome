import os

PORT = 8000 # application's port.
FACE_REG_MODEL_NAME = "VGG-Face" # model used for face recognition task.
IMAGE_DB_DIR = os.getcwd() + "/image-db" # directory to the folder storing face images.
